; Two executables sharing [broker_boot.ml] — dune's single stanza
; puts them in the same source namespace so both pick up the
; helper module automatically.

(executables
 (names main export_training_data train_logistic)
 (public_names trading trading_export_training_data trading_train_logistic)
 (package trading)
 (libraries core broker indicators strategies engine live_engine
            logistic_regression triple_barrier
            http_transport finam bcs token_store synthetic paper server log logs
            eio_main mirage-crypto-rng.unix unix))
