(test
 (name broker_unit_runner)
 (libraries
  core common decimal
  broker
  http_transport
  websocket
  finam
  bcs
  paper
  token_store
  acl_common
  eio
  eio_main
  str
  unix
  alcotest
  qcheck-core)
 (flags (:standard -open Common -w -33)))

; Same flatten-everything convention as test/unit/. Filenames
; carry [finam_] / [bcs_] prefix so flat namespace doesn't collide.
(include_subdirs unqualified)
