(** Placeholder module so the queries library compiles even with no
    public view-models yet. Outbound projections (kill-switch state,
    saga snapshots) will land here in a follow-up milestone. *)
