(library
 (name stream)
 (public_name trading.stream))

(rule
 (alias why3)
 (deps (glob_files *.mlw))
 (action
  (bash "set -eu
for f in %{deps}; do
  why3 prove -L . -P alt-ergo \"$f\" >/dev/null 2>&1 \\
    || why3 prove -L . -P z3 \"$f\" >/dev/null
done")))
