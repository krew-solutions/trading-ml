(** Why3 companion for [target_proposed.mli] — Domain Event record. *)

module Target_proposed
  use common.target_proposal.Target_proposal as TPR

  type z_score

  type t = {
    proposal : TPR.t;
    z        : z_score;
  }
end
