(test
 (name shared_unit_runner)
 (libraries bus in_memory core decimal rop workflow_engine alcotest qcheck-core eio eio_main))

; Same flatten convention as account/test/unit/. Filenames globally
; unique so the flat namespace doesn't collide.
(include_subdirs unqualified)
