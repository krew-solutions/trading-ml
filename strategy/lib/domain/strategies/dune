(library
 (name strategies)
 (public_name trading.strategies)
 (libraries core common decimal indicators gbt)
 (flags (:standard -open Common)))

(rule
 (alias why3)
 (deps
  (glob_files *.mlw)
  (glob_files ../common/*.mlw)
  (glob_files ../../../../shared/lib/domain/core/*.mlw)
  (glob_files ../../../../shared/lib/decimal/*.mlw)
  (glob_files ../indicators/*.mlw))
 (action
  (bash "set -eu
for f in %{deps}; do
  case \"$f\" in ../*) continue ;; esac
  why3 prove -L . -L ../common -L ../../../../shared/lib/domain/core -L ../../../../shared/lib/decimal -L ../indicators -P alt-ergo \"$f\" >/dev/null 2>&1 \\
    || why3 prove -L . -L ../common -L ../../../../shared/lib/domain/core -L ../../../../shared/lib/decimal -L ../indicators -P z3 \"$f\" >/dev/null
done")))

(alias
 (name runtest)
 (deps (alias why3)))
